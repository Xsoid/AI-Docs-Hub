---
title: Example Project
description: Derived placeholder page for the example project config.
---

This is a placeholder page for the sample project config in `configs/projects/example.yaml`.

It is not the source of truth for a real project. Replace the config `root` with a resolvable external project path, then keep that project's documentation inside that project.
