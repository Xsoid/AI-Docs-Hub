# 2026-09-23 - Постоянный стандарт гигиены документации

## Что изменилось

В хабе закреплен отдельный canonical standard для структуры документации и качества AI retrieval. Он определяет признаки кандидата на реорганизацию, границы `AGENTS.md`, `.agents/skills/` и `docs/`, правило одного canonical source, обработку конфликтов с кодом и проверки после реорганизации.

В `AGENTS.md` добавлена только маршрутизация к этому стандарту. Добавлены matching docs-site page и пункт навигации, чтобы policy находился как узким поиском по стандартам, так и через сайт.

## Проверка

- Проверен существующий `docs/standards/documentation.md`: 221 строка и 13.8 KB. Новый policy вынесен в отдельный документ, чтобы не расширять смешанный общий стандарт.
- Проверены ссылки и docs-site navigation.
- Регенерированы `llms*.txt`; сборка docs-site успешно создала страницу `/standards/documentation-hygiene/`.

## Follow-up

Нет.
